{
  "name": "locsynq",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first LAN-based bucket sharing platform",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev -w apps/backend",
    "dev:frontend": "npm run dev -w apps/frontend",
    "build:shared": "npm run build -w packages/shared",
    "build:frontend": "npm run build -w apps/frontend",
    "build": "npm run build:shared && npm run build:frontend",
    "start": "npm run start -w apps/backend",
    "clean": "rimraf apps/*/node_modules packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "rimraf": "^6.0.1"
  }
}
